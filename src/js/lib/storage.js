const appPrefix = '__se__todo__app__';

export function save(entity, data) {
    
};

export function get(entity, id) {
    
};

export function update(entity, id) {
    
};

export function delete(entity, id) {
  
};

function getAll(entity) {
    
};